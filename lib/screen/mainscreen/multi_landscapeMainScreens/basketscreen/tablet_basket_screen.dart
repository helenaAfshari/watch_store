import 'package:flutter/material.dart';

class TabletBasketScreen extends StatelessWidget {
  const TabletBasketScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold(body: Text("null Tablet"),));
  }
}