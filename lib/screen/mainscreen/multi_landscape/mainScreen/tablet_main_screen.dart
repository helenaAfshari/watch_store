import 'package:flutter/material.dart';

class TabletMainScreen extends StatelessWidget {
  const TabletMainScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold(body: Text("MainScreen"),));
  }
}