import 'package:flutter/material.dart';

class TabletCart extends StatelessWidget {
  const TabletCart({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
    child: Scaffold(body: Text(" Null TabletSize"),)
    );
  }
}