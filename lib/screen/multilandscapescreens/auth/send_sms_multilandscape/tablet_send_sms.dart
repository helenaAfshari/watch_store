
import 'package:flutter/material.dart';

class TabletSendSms extends StatelessWidget {
  const TabletSendSms({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
    child: Scaffold(body: Text("Null TabletSize"),)
    );
  }
}