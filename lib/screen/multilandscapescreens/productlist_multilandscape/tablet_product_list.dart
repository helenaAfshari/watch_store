import 'package:flutter/material.dart';

class TabletProductList extends StatelessWidget {
  const TabletProductList({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
    child: Scaffold(body: Text(" Null TabletSize"),)
    );
  }
}